.root
  appearance: none;
  border: 2px solid #000;
  border-radius: 3px;
  outline: none;
  transition: all .2s ease-out;
  cursor: pointer;

  &:hover
    border-color: rgba(0,0,0,.9);

  &:active
    transform: scale(.95);
