enum Role {
  USER
  ADMIN
}

scalar DateTime

type User {
  id: ID! @id
  joinedAt: DateTime! @createdAt
  name: String
  email: String! @unique
  password: String!
  roles: [Role!]! @default(value: USER)
}
