import { h, Fragment } from 'preact'

import { Title } from 'components/Header'

function Profile() {
  return (
    <Fragment>
      <Title text="Profile" />
      Your profile
    </Fragment>
  );
}

export default Profile;
