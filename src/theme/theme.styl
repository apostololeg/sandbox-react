$tablet = 500px
$desktop = 1200px

$indent_s = 10px
$indent_m = 20px
$indent_l = 30px

$accentColor = #333
$activeColor = #ffd52e

controlBorder(color = $accentColor)
  box-shadow inset 0 0 0 2px color
